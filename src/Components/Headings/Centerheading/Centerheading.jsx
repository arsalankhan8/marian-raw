import React from "react"; import { motion } from "framer-motion"; export default function Centerheading({ heading, subtext, headingcss = "", subtextcss = "", textColor = "text-white", }) { return ( <motion.div initial={{ opacity: 0, y: 50 }} whileInView={{ opacity: 1, y: 0 }} transition={{ duration: 1, ease: "easeOut" }} viewport={{ once: true, amount: 0.1 }} > <p className={`font-bold text-center uppercase ${textColor} ${headingcss}`} > {heading} </p> <p className={`font-normal text-center ${textColor} ${subtextcss}`} > {subtext} </p> </motion.div> ); }